services:
  postgres:
    image: postgres:16
    container_name: vendora_db
    restart: always
    environment:
      POSTGRES_USER: vendora
      POSTGRES_PASSWORD: vendora123
      POSTGRES_DB: vendora_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: vendora_redis
    restart: always
    ports:
      - "6379:6379"

volumes:
  postgres_data: